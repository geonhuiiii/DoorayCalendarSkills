# ──────────────────────────────────────
# Dooray Calendar Sync - 환경 변수 설정
# ──────────────────────────────────────
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요.
# cp env.example .env

# ─── Dooray (필수) ───
# CalDAV(caldav.dooray.com)를 사용합니다.
# 여러 인증 조합을 자동으로 시도합니다.
DOORAY_TENANT_ID=your-tenant-id
DOORAY_USERNAME=your-dooray-email@example.com
DOORAY_PASSWORD=your-dooray-password
# API 토큰 (비밀번호 대신 사용 가능, Dooray 개인설정에서 발급)
DOORAY_API_TOKEN=
# 멤버 ID (이메일 대신 사용 가능, Dooray 프로필에서 확인)
DOORAY_MEMBER_ID=
# 특정 캘린더 이름 (비워두면 기본 캘린더 사용)
DOORAY_CALENDAR_NAME=

# ─── Google Calendar (선택) ───
# Google Cloud Console에서 OAuth 2.0 클라이언트 생성 후 입력
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_CALENDAR_ID=primary

# ─── Apple Calendar (선택) ───
# Apple ID → 보안 → 앱 전용 비밀번호 생성 후 입력
# https://appleid.apple.com/account/manage
APPLE_USERNAME=your-apple-id@icloud.com
APPLE_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
APPLE_CALENDAR_NAME=
