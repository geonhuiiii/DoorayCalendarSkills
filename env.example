# ──────────────────────────────────────
# Dooray Calendar Sync - 환경 변수 설정
# ──────────────────────────────────────
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요.
# cp env.example .env

# ─── Dooray (필수) ───
# CalDAV를 사용합니다.
# 비밀번호는 로그인 비밀번호가 아닙니다!
# Dooray → 캘린더 → CalDAV 설정 페이지에서 발급된 CalDAV 전용 비밀번호를 입력하세요.
#
# 클라우드 환경 선택:
#   public  = 민간 클라우드       (caldav.dooray.com)
#   gov     = 공공 클라우드       (caldav.gov-dooray.com)
#   gov-kr  = 공공 업무망 클라우드 (caldav.gov-dooray.co.kr)
#   finance = 금융 클라우드       (caldav.dooray.co.kr)
DOORAY_CLOUD=gov
DOORAY_TENANT_ID=your-tenant-id
DOORAY_USERNAME=your-dooray-email@example.com
DOORAY_PASSWORD=your-caldav-password
# API 토큰 (선택, Dooray 개인설정에서 발급)
DOORAY_API_TOKEN=
# 멤버 ID (선택, Dooray 프로필에서 확인)
DOORAY_MEMBER_ID=
# 특정 캘린더 이름 (비워두면 기본 캘린더 사용)
DOORAY_CALENDAR_NAME=

# ─── Google Calendar (선택) ───
# Google Cloud Console에서 OAuth 2.0 클라이언트 생성 후 입력
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_CALENDAR_ID=primary

# ─── Apple Calendar (선택) ───
# Apple ID → 보안 → 앱 전용 비밀번호 생성 후 입력
# https://appleid.apple.com/account/manage
APPLE_USERNAME=your-apple-id@icloud.com
APPLE_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
APPLE_CALENDAR_NAME=
